<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!--android:shape=["rectangle" | "oval" | "line" | "ring"]
 	shape的形状，默认为矩形，可以设置为矩形（rectangle）、椭圆形(oval)、线性形状(line)、环形(ring)-->
    <!-- 实际上是创建了一个GradientDrawable对象。对象的形状可以是矩形、椭圆、线条和圆圈 -->
    <!--android:centerColor 整型   渐变中间颜色，即开始颜色与结束颜色之间的颜色
        android:angle       整型   渐变角度(PS：当angle=0时，渐变色是从左向右。 然后逆时针方向转，当angle=90时为从下往上。angle必须为45的整数倍)
        android:type        ["linear" | "radial" | "sweep"] 渐变类型(取值：linear、radial、sweep)
                            linear 线性渐变，这是默认设置
                            radial 放射性渐变，以开始色为中心。
                            sweep 扫描线式的渐变。-->
    <!--渐变-->
    <gradient
        android:angle="270"
        android:endColor="#989898"
        android:startColor="#EFEFEF"
        android:type="linear" />

    <!--单色 填充色 如果同时定义了渐变的话  效果会受影响-->
    <solid android:color="#FFF" />
    <!--角半径 定义所有4个角的半径 或者分别定义各个圆角的半径-->
    <corners android:radius="10dp" />
    <!--边线 对象形状的边界。可以定义边界的宽度和颜色-->
    <stroke
        android:width="5dp"
        android:color="#555" />
</shape>